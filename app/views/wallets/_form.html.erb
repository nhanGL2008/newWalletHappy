<%= form_for(@wallet) do |f| %>
  <% if @wallet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wallet.errors.count, "error") %> prohibited this wallet from being saved:</h2>

      <ul>
      <% @wallet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" style="padding-top: 8px; text-align: center;">
    <%= f.label :name %><br>
    <%= f.text_field :name , :placeholder => "Coins"%>
  </div>
  <div class="field" style="padding-top: 8px">
    <%= f.label :balance%><br>
    <%= f.text_field :balance , :placeholder => "1000 000$ " %>
  </div>
  <div class="actions btn btn-lg" style="padding: 8px 0px 8px 0px" >
    <%= f.submit %>
  </div>
<% end %>
