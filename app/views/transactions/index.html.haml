-# This is list of transaction

%h1 Listing Transactions
%table{class: %w(table table-striped sortable)}
  %thead
    %tr
      %td{:style => "text-align: center;"} Day
      %td{:style => "text-align: center;"} Balance
      %td{:style => "text-align: center;", :conspan => "2"} Category
      %td{:style => "text-align: center;"} Note

  %tbody
    -@transactions.each do |transaction|
      %tr
        %td= transaction.day
        %td= transaction.money
        %td= transaction.category.name
        %td= link_to transaction.note, transaction

        -#%td= Wallet.find_by_user_id(transaction.wallet_id)
    %end

= link_to 'New Transaction', new_transaction_path